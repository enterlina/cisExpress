const keyboard = (function () {
  'use strict';

  class Keyboard {
    constructor () {
      this.KEYS = {
        ENTER: 13
      };
    }
  }

  return new Keyboard();
})();

export default keyboard;
